import { YearcategoryfilterPipe } from './yearcategoryfilter.pipe';

describe('YearcategoryfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new YearcategoryfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
